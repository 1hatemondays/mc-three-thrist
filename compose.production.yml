services:
  maze-of-knowledge:
    container_name: maze-of-knowledge
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      TEAM_COUNT: 4
      HOST_ACCESS_KEY: ${HOST_ACCESS_KEY:?Set HOST_ACCESS_KEY in .env}
    ports:
      - "7020:8080"
    networks:
      - default
      - proxy
    restart: unless-stopped

networks:
  proxy:
    external: true
    name: mascoteach-network
